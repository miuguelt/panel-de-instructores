# Auto detect text files and perform LF normalization
* text=auto

# Scripts ejecutados dentro del contenedor Linux: con CRLF el kernel no
# encuentra el interprete y falla con "bad interpreter: No such file or directory".
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
